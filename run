#!/bin/sh
rm build -rf &&
echo ========================== compiling the code =========================== &&
mkdir -p ./build &&
cd build &&
cmake .. -DCMAKE_BUILD_TYPE=Debug &&
make &&
echo ========================== running the code ============================= &&
./rendering &&
cd ..
